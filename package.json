{
  "name": "heroclix-case-generator",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node tools/smoke.mjs && node tools/code-smoke.mjs && node tools/extras-smoke.mjs && node tools/api-smoke.mjs && node tools/booster-uniqueness-smoke.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "typescript": "^5.8.0"
  }
}
